<% provide :title, "Edit #{@user.email}" %>

<%= render 'shared/error_messages' %>

<%= form_for @user do |f| %>
	<%= f.label :email, "Email" %>
	<%= f.text_field :email %>
	
	<%= f.submit "Edit" %>
<% end %>
