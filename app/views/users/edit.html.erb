<% provide :title, "Edit #{@user.email}" %>

<%= form_for @user do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	
	<%= f.label :email, "Email" %>
	<%= f.text_field :email %>
	
	<%= f.submit "Edit" %>
<% end %>
